# パズルモード仕様書（Colordoku型・鳥配置パズル）

## 位置づけ

自室メニューの「パズル」から遊ぶ独立したモード。数字を埋める数独ではなく、色分けされた盤面で鳥のいるマスを推理して開くゲーム。

本書は目標仕様を記録する。試作実装の範囲は末尾に記す。

## 盤面と正解

- 盤面は N 行 × N 列で、N 個の色付き領域に分かれる。同じ色のマスはすべて上下左右の辺を通じてつながり、必ず 1 つの連結した領域を作る。斜め接触だけではつながったとみなさず、同じ色が離れた場所に分散する盤面は作らない。
- 正解の鳥は N 羽。各行、各列、各色領域にちょうど 1 羽ずついる。
- 鳥同士は縦・横・斜めの隣接マスに置かれない。
- 各問題は上記の条件を満たす正解が 1 通りになるように作る。
- ステージ作成時は各色の連結性と、鳥の配置の唯一解を検証する。
- 初期状態では鳥を一羽も表示しない。
- 1×1 は鳥 1 羽だけの自明な例外として成立する。鳥を複数置いて推理する盤面では 4×4 が最小。2×2 と 3×3 は、各行・列に 1 羽ずつ置くと鳥の非隣接条件を満たせない。

## プレイヤーの操作

- マスを 1 回タップすると、待ち時間なく通常の白い「×」メモを付ける。白い×が付いたマスを再び 1 回タップすると、すぐにそのメモを消す。白い×の付け外しではハートは減らない。
- マスをダブルタップすると、そのマスを「開いて」正解か判定する。1 回目のタップでは即時に×を付け外しし、2 回目で開いた結果（鳥か赤い×）に置き換える。2 回の単タップとして元のメモ状態へ戻す操作にはしない。操作方法は画面内で明示する。
- マスを押したままドラッグすると、最初のマスが空なら通過したマスに白い×を連続で付ける。最初のマスが白い×なら、通過したマスの白い×を連続で消す。ドラッグ中に処理済みのマスを再通過しても、付け外しを反転させない。発見済みの鳥と赤い×は変更しない。
- 白い×メモが付いたマスもダブルタップで開ける。開いた後は白い×を鳥または赤い×の結果表示に置き換える。
- 鳥を発見しても、同じ行・列・色領域のほかのマスに白い×を自動で付けない。候補の消去はプレイヤーが行う。
- 正解のマスを開いた場合は鳥を表示し、発見数を増やす。ハートは減らない。
- **鳥がいないマスを開いた場合は、ハートを 1 つ減らし、そのマスを強制的な赤い「×」にする。** 赤い×は通常の白い×メモと区別し、プレイヤーは消せず、そのマスを再び開けない。
- 開いたマスが現在の鳥との行・列・色・隣接条件に明白に反していなくても、正解に鳥がいなければ誤答として扱う。ハートの判定は、部分配置との矛盾判定ではなく、その問題の正解データに基づく。
- 正解の鳥を N 羽すべて見つけたらクリア。クリア演出を表示した後、次のレベルの盤面へ進む。
- ハートが 0 になったらゲームオーバーとし、自室のメニュー画面へ戻る。
- ゲームオーバー後に再度「パズル」へ入ると、失敗した同じレベルを最初から始める。盤面と正解は同じまま、発見した鳥、白い×メモ、赤い×、ハートなど、その挑戦中の状態は初期状態へ戻す。

## 画面表示

- 現在の発見数を「発見した鳥 / N」で表示する。
- 残りハートを表示する。初期値は添付スクリーンショットに合わせて 3 とする。
- 通常の白い×メモ、誤答による赤い×、発見した鳥を見た目で明確に区別する。
- 色領域、各行・列、鳥同士の非隣接という 3 条件を、盤面と同時に確認できるようにする。
- 初期版にはヒント機能・ヒントボタンを設けない。

## 未確定事項

- 各サイズ内の正式な問題数と難易度調整、スコアや報酬と本編との関係。
- クリア演出の具体的な見せ方と所要時間。

## 試作実装

- レベル 1〜5 は 7×7、6〜10 は 8×8、11〜15 は 9×9、16 以降は 10×10。盤面全体の幅を一定に保ち、マス・鳥・×をサイズに合わせて縮小する。
- 7×7 は 2 種、8×8・9×9・10×10 は各 1 種の試作テンプレートを用意した。鳥の初期公開はなく、全盤面で色領域の辺連結と唯一解を検証済み。
- 盤面サイズごとに検証済みテンプレートを反転・回転して 5 レベル分用意し、Lv.1〜5 は 7×7、Lv.6〜10 は 8×8、Lv.11〜15 は 9×9、Lv.16 以降は 10×10 とする。10×10 は以降も変形盤面を循環使用するため、長期的な問題数と難易度設計は今後決める。
- 白い×メモ、ダブルタップでの開封、誤答時の赤い×とハート減少、ゲームオーバー時の自室帰還、クリア演出後の自動進行を実装。ヒントと初期公開の鳥は設けない。
- 対応ブラウザでは、マスを開く・×を付ける・消すときに短い振動を要求する。iPhone SafariはVibration APIに対応しないため、実機の振動にはならない。
- レベル進行はクリア時に現在のセーブ枠へ保存する。挑戦途中のメモやハートは保存しないため、再入場時はそのレベルの初期状態から始まる。

参考：[Colordoku: Logic Puzzle の公式 Google Play 掲載説明](https://play.google.com/store/apps/details?id=com.clarkstudio.an.queen.colordoku)。鳥の配置条件、×によるメモ、ダブルタップ、問題ごとの唯一解を確認。赤い×とハートの挙動はユーザー提供スクリーンショットおよび今回の補足を優先する。
